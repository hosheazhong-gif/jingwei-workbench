# 安全说明

## 运行边界

经纬是单机应用，只允许监听 IPv4 回环地址或 `localhost`，并拒绝其他网页发起的跨站写请求。不要通过反向代理、端口转发或防火墙规则把本地服务暴露到局域网或公网。

用户数据库、材料副本和导出默认保存在 `%LOCALAPPDATA%\Jingwei`。这些文件可能包含敏感项目资料，应按所在组织的数据管理要求备份、加密和删除。

## 报告漏洞

请使用 GitHub 仓库 Security 页面中的私密漏洞报告功能。不要在公开 Issue 中附带客户资料、数据库、API Key、访问令牌或可利用细节。

报告中请包含受影响版本、复现条件、潜在影响和建议修复方向。一般功能问题仍使用普通 Issue。
