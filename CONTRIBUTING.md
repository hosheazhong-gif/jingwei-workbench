# 参与开发

欢迎修复问题、补充测试、改进交互或新增通用模板。

## 提交前

1. 先在 Issue 中说明问题或拟议改动；小型明确修复可直接提交 Pull Request。
2. 从 `main` 创建短分支，保持一次提交只解决一个清楚的问题。
3. 为行为变化补测试，并运行 `python -m unittest discover -s tests -v`。
4. 若改动前端，运行 `node --check frontend/report/app.js` 并实际打开页面检查。
5. 若改动封装，运行 `python scripts/build_windows_app.py`，不能只验证源码入口。

## 内容与数据

- 不要提交真实客户资料、项目数据库、受控副本、导出稿或未经脱敏的截图。
- 不要提交 `.env`、API Key、访问令牌或个人目录。
- 新模板应描述通用工作边界，不应把单个项目结论包装成方法论。
- 涉及事实与数字的测试应使用明确标注的合成数据。

## Pull Request

请写清：改了什么、为什么、如何验证、是否影响数据库迁移或用户数据。CI 必须全部通过。
